<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Admin Panel</title>

    <!-- Fonts -->
    <link rel="dns-prefetch" href="//fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css?family=Nunito" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">

    <!-- Styles -->
    <link href="/css/app.css" rel="stylesheet">
</head>
<body>

<div id="app">
    <nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="{{ path('admin.home') }}">
                Bedrock Dedicated Server
            </a>
            <button
                    class="navbar-toggler"
                    type="button"
                    data-toggle="collapse"
                    data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent"
                    aria-expanded="false"
            >
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link {{ uriEquals(path('admin.home')) ? 'active' : ''}}" href="{{ path('admin.home') }}">
                            Home
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link {{ uriEquals(path('admin.whitelist.index')) ? 'active' : ''}}" href="{{ path('admin.whitelist.index') }}">
                            Whitelist
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link {{ uriEquals(path('admin.properties.index')) ? 'active' : ''}}" href="{{ path('admin.properties.index') }}">
                            Properties
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin.home') }}">
                            World and backups
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin.info.index') }}">
                            Info
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="app-content py-3">
        <div class="container">
            {% block breadcrumbs %}{% endblock %}
            {% block body %}{% endblock %}
        </div>
    </main>
</div>

<script
        src="https://code.jquery.com/jquery-3.5.1.min.js"
        integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
        crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

</body>
</html>